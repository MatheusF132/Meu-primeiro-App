import { StyleSheet } from 'react-native';

export const saveButtonStyle = StyleSheet.create({
  button: {
    width: 200,
    height: 45,
    left: 185,
    top: -50,
  },

  text: {
    color: 'blue',
  },
});